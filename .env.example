# Set a unique password with at least 12 characters in Netlify environment variables.
# Scope must include Functions. Redeploy after any change. Never commit real passwords.
ADMIN_PASSWORD=

# Optional: also protect the dashboard/map with a separate viewing password.
# Leave empty for a public dashboard and password-protected administration.
JOURNAL_PASSWORD=
